/// Marks a repository interface for use-case generation.
///
/// Use `smart_domain_generator` with `build_runner` to generate one use case
/// for every supported repository method.
final class GenerateUseCases {
  const GenerateUseCases();
}

## 0.1.0

- Added `@GenerateUseCases()` repository generation.
- Added generated parameter objects for repository method arguments.
- Reused existing parameter types ending in `Params`.
- Added parameterless generation through `NoParamsUseCase`.
